{
  "name": "openvrhorse",
  "version": "1.0.0",
  "description": "Interface WebGL Three.js — sélection d'allure (pas, trot, galop)",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "build:pages": "vite build --mode pages",
    "preview": "vite preview",
    "preview:pages": "vite preview --mode pages"
  },
  "dependencies": {
    "three": "^0.172.0",
    "uisfx": "^0.4.0"
  },
  "devDependencies": {
    "vite": "^6.0.7"
  },
  "license": "MIT"
}
